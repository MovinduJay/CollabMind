apiVersion: v1
kind: Service
metadata: { name: web, namespace: collabmind }
spec: { selector: { app.kubernetes.io/name: web }, ports: [{ name: http, port: 80, targetPort: http }] }
---
apiVersion: v1
kind: Service
metadata: { name: identity-service, namespace: collabmind }
spec: { selector: { app.kubernetes.io/name: identity-service }, ports: [{ name: http, port: 8082, targetPort: http }] }
---
apiVersion: v1
kind: Service
metadata: { name: chat-core, namespace: collabmind }
spec: { selector: { app.kubernetes.io/name: chat-core }, ports: [{ name: http, port: 8081, targetPort: http }] }
---
apiVersion: v1
kind: Service
metadata: { name: realtime-gateway, namespace: collabmind }
spec: { selector: { app.kubernetes.io/name: realtime-gateway }, ports: [{ name: http, port: 8083, targetPort: http }] }
---
apiVersion: v1
kind: Service
metadata: { name: ai-orchestrator, namespace: collabmind }
spec: { selector: { app.kubernetes.io/name: ai-orchestrator }, ports: [{ name: http, port: 8084, targetPort: http }] }
---
apiVersion: v1
kind: Service
metadata: { name: tool-mcp-server, namespace: collabmind }
spec: { selector: { app.kubernetes.io/name: tool-mcp-server }, ports: [{ name: http, port: 8085, targetPort: http }] }
